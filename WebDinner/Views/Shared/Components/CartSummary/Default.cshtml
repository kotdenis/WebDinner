@model Cart
<div class="">
    @if (Model.FirstDishes.Count() > 0)
    {
        <small class="navbar-text">
            <b>Your cart:</b>
            @Model.FirstDishes.Sum(x => x.Quantity) item(s)
            @Model.ComputeTotalValue().ToString("c")
        </small>
    }
    @if (Model.SecondDishes.Count() > 0)
    {
        <small class="navbar-text">
            <b>Your cart:</b>
            @Model.SecondDishes.Sum(x => x.Quantity) item(s)
            @Model.ComputeTotalValue().ToString("c")
        </small>
    }
    @if (Model.Salads.Count() > 0)
    {
        <small class="navbar-text">
            <b>Your cart:</b>
            @Model.Salads.Sum(x => x.Quantity) item(s)
            @Model.ComputeTotalValue().ToString("c")
        </small>
    }
    <a class="btn btn-sm btn-secondary navbar-btn"
       asp-controller="Cart" asp-action="Index"
       asp-route-returnurl="@ViewContext.HttpContext.Request.PathAndQuery()">
        <i class="fa fa-shopping-cart"></i>
    </a>
</div>